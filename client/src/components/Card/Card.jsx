const Card = (props)=>{
    return (
     <div>
        <p>name:{props.name}</p>
        <p>tipo:{props.type}</p>
        <p>img :{props.url}</p>
        <p></p>
    
    
    
     </div>
    
    )
    
    }
     
    export default Card